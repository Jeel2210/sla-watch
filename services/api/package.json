{
  "name": "api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.mjs --external:pg-native \"--banner:js=import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "@sla/core": "^0.1.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "@types/node": "^22.20.4",
    "@types/pg": "^8.11.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.3.0",
    "vitest": "^5.0.2"
  }
}
